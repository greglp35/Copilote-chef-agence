info:
  name: Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/health"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("status is successful", function() {
          expect(res.status).to.be.within(200, 299);
        });

settings:
  encodeUrl: true
